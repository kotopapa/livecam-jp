diff --git a/app/lib/config.dart b/app/lib/config.dart
index 7801387..fe5ab13 100644
--- a/app/lib/config.dart
+++ b/app/lib/config.dart
@@ -7,6 +7,10 @@ const String manifestUrl = '${apiBaseUrl}manifest.json';
 /// アプリのバージョン表記（pubspec.yaml の version と一致させる）。
 const String appVersion = '1.5.0';
 
+/// ストア用スクリーンショット撮影モード（`--dart-define=SCREENSHOT_MODE=true`）。
+/// デバッグリボンと広告を出さず、ATT ダイアログも出さない。通常ビルドでは false
+const bool screenshotMode = bool.fromEnvironment('SCREENSHOT_MODE');
+
 /// アクセス制御（SPEC 9.4）。ユーザーが変更できない下限値。
 const Duration minRefetchInterval = Duration(seconds: 60);
 const int maxConcurrentFetches = 3;
diff --git a/app/lib/main.dart b/app/lib/main.dart
index ab83db5..7968762 100644
--- a/app/lib/main.dart
+++ b/app/lib/main.dart
@@ -16,6 +16,7 @@ import 'firebase_options.dart';
 import 'package:path_provider/path_provider.dart';
 
 import 'app_state.dart';
+import 'config.dart' show screenshotMode;
 import 'data/api_client.dart';
 import 'data/cache_store.dart';
 import 'data/camera_repository.dart';
@@ -73,7 +74,7 @@ Future<void> main() async {
   app.init(); // キャッシュ復元→バックグラウンド更新（待たずに起動する）
   Future.delayed(
       const Duration(milliseconds: 1600), FlutterNativeSplash.remove);
-  _initAds(); // 起動をブロックしない（ATT許可→AdMob初期化）
+  if (!screenshotMode) _initAds(); // 起動をブロックしない（ATT許可→AdMob初期化）
 }
 
 /// 広告の初期化。ATT（トラッキング許可）の回答を待ってからSDKを起動する。
@@ -142,6 +143,7 @@ class _LiveCamAppState extends State<LiveCamApp>
       listenable: widget.localeController,
       builder: (context, _) => MaterialApp(
         onGenerateTitle: (context) => context.l10n.appTitle,
+        debugShowCheckedModeBanner: !screenshotMode,
         theme: ThemeData(colorSchemeSeed: const Color(0xFF1E6FD9)),
         locale: widget.localeController.locale,
         supportedLocales: AppLocalizations.supportedLocales,
diff --git a/app/lib/ui/ad_banner.dart b/app/lib/ui/ad_banner.dart
index c99f855..238fc08 100644
--- a/app/lib/ui/ad_banner.dart
+++ b/app/lib/ui/ad_banner.dart
@@ -65,8 +65,8 @@ class _AdBannerPlaceholderState extends State<AdBannerPlaceholder> {
   @override
   Widget build(BuildContext context) {
     // 失敗時は区切り線ごと消す（前後のDividerが二重にならないように
-    // 下側の区切り線はこのウィジェットが持つ）
-    if (_failed) return const SizedBox.shrink();
+    // 下側の区切り線はこのウィジェットが持つ）。撮影モードでは出さない
+    if (screenshotMode || _failed) return const SizedBox.shrink();
     return VisibilityDetector(
       key: Key('ad-${widget.adUnitId}-${widget.size.height}-${identityHashCode(this)}'),
       onVisibilityChanged: _onVisibility,
@@ -147,7 +147,7 @@ class _AnchoredAdBannerState extends State<AnchoredAdBanner> {
 
   @override
   Widget build(BuildContext context) {
-    if (_failed) return const SizedBox.shrink();
+    if (screenshotMode || _failed) return const SizedBox.shrink();
     if (_ad == null) {
       WidgetsBinding.instance.addPostFrameCallback((_) => _load(context));
       return const SizedBox.shrink();
diff --git a/app/lib/ui/list_screen.dart b/app/lib/ui/list_screen.dart
index 42ca05e..004b581 100644
--- a/app/lib/ui/list_screen.dart
+++ b/app/lib/ui/list_screen.dart
@@ -2,6 +2,7 @@ import 'package:flutter/material.dart';
 import 'package:geolocator/geolocator.dart';
 
 import '../app_state.dart';
+import '../config.dart' show screenshotMode;
 import '../l10n/l10n.dart';
 import '../models/camera.dart';
 import '../util/geo.dart';
@@ -57,7 +58,8 @@ class _ListScreenState extends State<ListScreen> {
     _lastAttempt = DateTime.now();
     try {
       var permission = await Geolocator.checkPermission();
-      if (permission == LocationPermission.denied && request) {
+      // 撮影モードでは起動時に OS の許可ダイアログを出さない（初回描画を止めるため）
+      if (permission == LocationPermission.denied && request && !screenshotMode) {
         permission = await Geolocator.requestPermission();
       }
       if (permission == LocationPermission.denied ||
diff --git a/app/pubspec.yaml b/app/pubspec.yaml
index ffe4a77..2552b1e 100644
--- a/app/pubspec.yaml
+++ b/app/pubspec.yaml
@@ -77,6 +77,8 @@ dev_dependencies:
   # package. See that file for information about deactivating specific lint
   # rules and activating additional ones.
   flutter_lints: ^6.0.0
+  integration_test:
+    sdk: flutter
 
 # For information on the generic Dart part of this file, see the
 # following page: https://dart.dev/tools/pub/pubspec
