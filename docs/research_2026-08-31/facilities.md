# 防災拠点データ（給水拠点・防災備蓄倉庫・消防水利）調査記録

調査日: 2026-08-31 / 実装: `tools/facilities.py`, `.github/workflows/facilities.yml`

## 1. 結論（先に読む）

- **国による全国集約は存在しない**。避難場所（国土地理院 hinanmap）のような「1本のCSVを落とせば全国が揃う」データソースは、
  給水拠点・防災備蓄倉庫・消防水利・災害時給水栓のいずれにも無い。
- デジタル庁の**「自治体標準オープンデータセット」に様式（`消防水利施設一覧` など）はあるが、データ本体の全国集約は無い**。
  様式が決まっているおかげで**列名がほぼ統一されている**ので、集めさえすれば正規化は容易。
- したがって現実解は **CKAN横断検索**。最大の受け皿が **BODIK ODCS (`data.bodik.jp`)**（388組織・18,392データセット、CKAN API 認証不要）で、
  東京都は独自CKAN (`catalog.data.metro.tokyo.lg.jp`) を持つ。
- 実装した `tools/facilities.py` は CKAN の `package_search` を `title:` フィールド検索で叩き、
  タイトルで種別を判定 → ライセンスを判定 → CSVを取得 → 正規化 → `data/facilities/<JIS2桁>.json` に県別出力する。
- **2026-08-31 第2回**: XLSX対応と山口県ポータル追加で **148,871件 → 339,670件（22県→24県・78→91自治体）**。
  伸びの9割は東京消防庁の消火栓・防火水槽XLSX（178,255件・原データに座標あり）。
- **量は消防水利に極端に偏る**（採用データセットの9割）。給水拠点は数十件、備蓄倉庫はほぼ皆無。
  これはデジタル庁標準様式に「消防水利施設一覧」があり「給水拠点一覧」「備蓄倉庫一覧」が無いことの直接の帰結。

## 2. どのCKANに何があるか

### 2.1 採用したポータル

| ポータル | ベースURL | robots.txt | API | 備考 |
|---|---|---|---|---|
| BODIK ODCS | `https://data.bodik.jp` | `/api/` 許可・`Crawl-delay: 2` | CKAN 3 | 388組織。九州中心だが全国の市町村が参加。組織の `name` が全国地方公共団体コード6桁なので県コードを引ける |
| 東京都オープンデータカタログ | `https://catalog.data.metro.tokyo.lg.jp` | robots.txt が 403（CloudFront既定・全UAで取得不可）→ RFC 9309 に従い制限なしとして扱う | CKAN 3 | 都本庁・水道局・消防庁・特別区・多摩市部が同居。CSV実体は各区市の自ドメインに置かれている（robots.txtはそのホストごとに確認している） |

### 2.2 追加調査（2026-08-31 第2回）— なぜ自治体CKANはほとんど使えないのか

全国のCKANを洗い直した結果、**素のCKANを立てている自治体はほぼ全部クロールできない**ことが分かった。
理由は単純で、**CKAN の配布時 robots.txt が `User-agent: *` に `Disallow: /api/` を含んでいる**から。
札幌市・横浜市が禁止だったのは個別の判断ではなく、CKANの既定値をそのまま使っているだけだった。

逆に **BODIK が使えるのは、BODIK が robots.txt を書き換えていて
`Disallow: /api/` を Sogou / Baiduspider / Bytespider / CCBot のグループにしか付けていない**から
（`User-agent: *` 側は `/user/ /admin/ /dataset/*?*` などのみ）。整合が取れている。

以下は 2026-08-31 に1件ずつ `robots.txt` を取得して確認した結果で、すべて `BLOCKED_PORTALS` に記録した。
**許諾が得られれば `PORTALS` に1行移すだけで有効化できる。**

| ポータル | ベースURL | robots.txt |
|---|---|---|
| DATA-SMART CITY SAPPORO | `ckan.pf-sapporo.jp` | `Disallow: /api/`（`Crawl-Delay: 10`） |
| 横浜市 | `data.city.yokohama.lg.jp` | `Disallow: /api/` |
| 岐阜県 | `gifu-opendata.pref.gifu.lg.jp` | `Disallow: /api/`（1,880データセット） |
| 神奈川県 | `catalog.opendata.pref.kanagawa.jp` | `Disallow: /api/`（811） |
| 秋田県 | `ckan.pref.akita.lg.jp` | `Disallow: /api/`（264） |
| ODP（鯖江市ほか・jig.jp） | `ckan.odp.jig.jp` | `Disallow: /api/`, `/*?`（1,068） |
| DATA for CITIZEN（会津若松市ほか） | `data.data4citizen.jp` | `Disallow: /api/`（411） |
| 新潟市 | `opendata.city.niigata.lg.jp` | `Disallow: /api/`（487） |
| 港区 | `opendata.city.minato.tokyo.jp` | `Disallow: /api/`（560） |
| 宇都宮市 | `catalog.city.utsunomiya.tochigi.jp` | `Disallow: /api/`（295） |
| 金沢市 | `catalog-data.city.kanazawa.ishikawa.jp` | `Disallow: /api/`, `/datastore/`（320） |
| 町田市 | `opendata.city.machida.tokyo.jp` | `Disallow: /api/`（143） |
| 富山市 | `opdt.city.toyama.lg.jp` | `Disallow: /api/`（133） |
| 相模原市 | `opendata.city.sagamihara.kanagawa.jp` | `Disallow: /api/`（114） |
| 天童市 | `data.city.tendo.yamagata.jp` | `Disallow: /api/`, `Crawl-Delay: 10`（10） |
| 洲本市 | `data.city.sumoto.lg.jp` | `Disallow: /api/`（5） |
| G空間情報センター | `www.geospatial.jp/ckan` | `Disallow: /*?*`＝**クエリ付きURL全面禁止**なので `package_search` 自体が不可（17,286） |

→ SPEC 2章 C4 / 10章に従い**クロールしない**。

### 2.3 追加したポータル: 山口県

| ポータル | ベースURL | robots.txt | 備考 |
|---|---|---|---|
| 山口県オープンデータカタログサイト | `https://yamaguchi-opendata.jp/ckan` | `/dashboard` と `/ckan/dataset?*`・`/ckan/*/group` 等のみ。**`/api/` の記載なし**＝抵触しない | 素のCKAN。組織の `name` が全国地方公共団体コード5桁なので BODIK と同じ `org_name_is_jis` が使える。`title:消防水利` で14件（下関市・美祢市・柳井市・下松市・光市ほか）。**山口県は未カバー県だった** |

### 2.4 CKAN互換だが独自API（`/ckan_api/`）— dataeye 系。**ライセンス欄が無いため不採用**

埼玉県・岡山県・広島県・島根県・宮城県が同じ「dataye」プラットフォームを使っており、
`https://<host>/ckan_api/package_search?q=消防水利` が認証なしで動く。**robots.txt は全面許可**。
未カバー県を5つ埋められる非常に有望な鉱脈だが、**パッケージにライセンスを表す項目が1つも無い**
（`license_id` / `license_title` / `license_url` が無く、`extras` にも description・contactPoint・
release_date しか入っていない）。SPEC 2章 C5 の「不明は採用しない」に該当するため今回は入れていない。

| ポータル | ベースURL | `q=消防水利` | 中身の例 |
|---|---|---:|---|
| 埼玉県 | `opendata.pref.saitama.lg.jp/ckan_api` | 14 | さいたま市・上尾市・川口市・川越市・上里町 |
| 岡山県 | `www.okayama-opendata.jp/ckan_api` | 5 | 倉敷市・津山市・岡山市・総社市 |
| 広島県（広島広域都市圏。山口県の市町も相乗り） | `hiroshima-opendata.dataeye.jp/ckan_api` | 14 | 三原市・岩国市・柳井市・上関町・平生町 |
| 島根県 | `shimane-opendata.jp/ckan_api` | 1 | 安来市 |
| 宮城県（県＋市町村共同） | `miyagi.dataeye.jp/ckan_api` | 14 | 仙台市・名取市・塩竈市・多賀城市・亘理町 |

→ **次の一手はここ**。各ポータルの利用規約（サイト全体でCC BY等を宣言していることが多い）を確認して
ポータル単位のライセンスを根拠付きで持たせられれば、5県・数十自治体が一気に入る。
データセット単位のライセンスが取れないままの機械的な採用はしない。

### 2.5 CKANではなかったもの / 使えなかったもの

- **`www.data.go.jp/data`（デジタル庁 データカタログサイト）**: CKAN API は 200 を返し 18,141件あるが、
  **`q` パラメータが完全に無視される**（`q=title:消防水利` でも `q=消防水利` でも count が 18,141 のまま、
  先頭は毎回「第366回 個人情報保護委員会」）。キーワードで絞れないので全件ページング（182リクエスト×クエリ数）
  が必要になり、カタログへの負荷が正当化できない。robots.txt は404（制限なし）なので、
  絞り込み方法が分かれば使える。
- **SHIRASAGI系（青森県・静岡県・香川県・鳥取県・愛媛県・徳島県・北海道HARP）**: `/api/3/action/` は404で、
  代わりに `/api/package_list`・`/api/package_search` という**独自のCKAN風API**。別実装が要る。
  なお香川県は `robots.txt` が `User-agent: * / Disallow: /` で**全面禁止**なのでそもそも対象外。
- **加古川市** `opendata-api-kakogawa.jp/ckan`: 素のCKAN・robots.txt 404（制限なし）だが全80データセットで
  今回のキーワードに該当なし。
- **`odcs.bodik.jp/<コード>/`（日進市・栃木県・大阪府・京都府ほか多数）**: BODIK配下の自治体別サブサイトで、
  実体は採用済みの `data.bodik.jp` に含まれる。
- 千葉市（独自PHP API）、千葉県・川崎市・浜松市（HTMLのみ）、福井県（LinkData）は CKAN ではない。
- 消滅ドメイン: `opendata.pref.toyama.jp`、`ckan.pref.shizuoka.jp`、`opendata.city.kazuno.akita.jp`。

※ 調査には NII の横断検索 `https://search.ckan.jp/sites`（ハーベスト元のCKAN一覧）が有効だった。

## 3. 検索クエリの設計（重要）

CKAN(Solr) の日本語全文検索は形態素解析で無関係なデータセットまで拾う。実測（BODIK, 2026-08-31）:

| クエリ | ヒット数 |
|---|---|
| `q=災害時給水`（全文） | 1,375 |
| `q=防災備蓄`（全文） | 705 |
| `q=給水施設`（全文） | 2,356 |
| `q=title:消防水利` | 72 |
| `q=title:応急給水` | 3 |

全文検索は 13クエリで **約60リクエスト**（100件ページング）を要して分類後87件、
`title:` 検索は **13リクエスト**で同じ87件（差は彦根市「災害備蓄品一覧」1件のみ。これは `災害備蓄` をクエリに追加して回収）。
→ **`title:<キーワード>` 検索を採用**。カタログへの負荷が1/5になる。

`SEARCH_QUERIES`: 消防水利 / 消火栓 / 防火水槽 / 給水拠点 / 応急給水 / 災害時給水 / 給水ステーション /
耐震性貯水槽 / 飲料水兼用貯水槽 / 緊急貯水槽 / 備蓄倉庫 / 防災倉庫 / 備蓄物資 / 防災備蓄 / 災害備蓄

## 4. 種別の判定と、あえて採らなかったもの

`classify()` はデータセットのタイトルだけで判定する（本文やタグは自治体ごとに書式がバラバラで信用できない）。

- `stock` … 備蓄倉庫 / 防災倉庫 / 備蓄物資 / 防災備蓄 / 備蓄品 / 備蓄食料
- `water` … 給水拠点 / 応急給水 / 災害時給水 / 給水ステーション / 耐震性貯水槽 / 飲料水兼用貯水槽 / 緊急貯水槽 / 災害用井戸
- `fire_water` … 消防水利 / 消火栓 / 防火水槽

除外キーワード（`TITLE_EXCLUDE`）で統計表・名簿を落とす: 事業者 / 業者 / 名簿 / 工事店 / 統計 / 年鑑 / 統計書 / 推移 /
普及 / 料金 / 人口 / 件数 / 面積 / レポート / 指針 / カルテ / の数 / 状況 / 計画 / 収支 / 実績 / 使用量 / 有収 / 供給水量 / 整備率 / 充足率。

**あえて採らなかったもの（判断の記録）**

| 対象 | 判断 |
|---|---|
| 「給水スポット一覧」（福岡市ほか） | マイボトル用の**平常時の給水スポット**であり災害時給水拠点ではない。混同すると危険なので除外 |
| 「給水施設」（鹿児島市、配水池等） | 水道施設台帳であり応急給水拠点とは限らない。`water` に混ぜると誤解を招くので除外 |
| 「貯水槽一覧」（墨田区）ほか修飾語なしの貯水槽 | 防火水槽なのか耐震性貯水槽（飲料水）なのか判別できない。`耐震性`/`飲料水兼用`/`緊急` が付くものだけ採用 |
| 「〜(施設カルテ)」（調布市の防災倉庫3件） | 1施設1ファイルの施設カルテXLSXで一覧ではない |

## 5. ライセンス判定（SPEC 2章 C5 / 3.3）

`license_status()` は `license_id` + `license_title` の文字列で判定する。

- **先に否定判定**: `non-commercial` / `-nc` / `no-deriv` / `-nd` / 「禁止」「不可」→ 不採用。
  `cc-by-nc` は `cc-by` を含むので、順序を逆にすると事故る（テスト `test_license_nc_beats_by` で固定）。
- **採用**: `cc-by`系 / `cc0`・`cc-zero`・public domain / `PDL 1.0` / 政府標準利用規約・公共データ利用規約 / `odc-by`
- **不採用**: `other-copyright`、`notspecified`、`gfdl`（継承条件が強く再配布条件を機械判定できない）、ライセンス未記載

BODIK 全体（今回のキーワードで拾った5,170件）のライセンス分布は
`cc-by-40-intl` 3,981 / `cc-by` 838 / `PDL1.0` 104 / `CC-BY-4.0` 100 / `cc-by-21-jp` 71 / `cc-by-21` 46 /
`other-copyright` 12 / `Truecc-by-40-intl` 5（登録側の打ち間違い。`cc-by` として採用される）/ `gfdl` 4 /
`cc-zero` 3 / `odc-by` 3 / `notspecified` 2 / `cc-nc` 1。
→ **実質すべて CC BY**。今回の採用対象でライセンス起因の不採用は0件だった（除外理由はすべて「CSVリソースなし」）。

出典は `data/facilities/index.json` の `sources[]`（自治体名・データセットタイトル・データセットページURL・ライセンス・更新日時）と、
県別ファイルの `sources[]` に必ず残る。レコードの `s` はその配列の添字。

## 6. 正規化の実装メモ（自治体CSVの現実）

- **文字コードは UTF-8(BOM付) と CP932 が半々**。`decode_csv()` が `utf-8-sig` → `cp932` → `utf-8` の順に試す。
- **列名の表記ゆれ**は NFKC 正規化＋空白除去で吸収（`所在地＿連結標記` → `所在地_連結標記`、`建物名等（方書）` → `建物名等(方書)`、`Ｎｏ` → `No`）。
  「連結**表**記」と「連結**標**記」の両方が実在するので両方を候補に入れている。
- **緯度経度の列名**は `緯度` 固定ではない: `世界緯度10進`（船橋市）、`緯度（１０進法表記）`（高島市）、
  `緯度(栓)(座標)`（彦根市）、`緯度（世界測地系WGS84 10進数）`（豊田市）。→ 完全一致 → 「緯度を含む列」の順で拾い、
  `度分秒`/`日本測地系`/`旧`/`X座標`/`Y座標` を含む列は除外。
- **緯度と経度が入れ替わっている自治体がある**ため、値のレンジで判定して入れ替える（`parse_coord`）。
- **名称列が無いCSVが多い**（消火栓は名前が無いのが普通）。その場合は `種別` 列（消火栓/防火水槽/プール/河川など）を名称に使う。
- **住所列が無いCSVも多い**。標準様式の `所在地_都道府県` + `所在地_市区町村` + `所在地_町字` + `所在地_番地以下` を連結して復元する。
- **同一データセットに年度違いのCSVが複数ぶら下がっている**（福知山市4本、箕面市3本、高島市3本、寝屋川市4本、上天草市3本など）。
  どれが最新か機械判定できないので**全部読んで座標＋名称＋種別で重複排除**している（`split_by_pref`）。

## 6.5 XLSX/XLS への対応（2026-08-31 追記）

初回収集で不採用になった17件のうち13件がXLSX/XLSだった。`openpyxl` を requirements に足し、
`read_xlsx()` / `resource_format()` / `xlsx_error()` を実装して回収した。

- **リソース形式は `format` 欄ではなく URL の拡張子を優先**して判定する（`resource_format`）。
  BODIK には format が空で実体が `.xlsx`（久山町「消火栓」）、format が `XLS` で実体が `.xlsx`
  というリソースが実在する。拡張子が無いダウンロードURLのときだけ format 欄を見る。
- **ヘッダ行は先頭とは限らない**。「※この表は令和7年4月1日現在…」のような説明行が数行入る。
  先頭25行（空行を除く）から「名称/所在地/緯度/経度/種別/水利/番号/口径…」といった
  ヘッダ語に当たった列数が最大の行をヘッダとみなす（`_header_score`）。
  説明文にヘッダ語が入っていても数えないよう、30文字を超えるセルは候補にしない。
- **複数シートを全部読んで連結する**（消火栓シート／防火水槽シート、消防署ごとのシート）。
  ヘッダ語のスコアが2未満のシート（凡例・グラフ・集計表）は誤読を避けて捨てる。
- **数値セルは `462209.0` にしない**（`_cell_text`）。全国地方公共団体コードや水利番号が
  Excel上は数値で入っており、そのまま str() すると県コード判定に失敗する。
- **旧形式のXLS（BIFF, `D0 CF 11 E0` で始まる）は openpyxl では読めない**。
  ダウンロード後にマジックナンバーで判定し（`xlsx_error`）、
  「旧形式のXLS（openpyxl非対応）」を rejected の理由に残してスキップする（厚木市1件）。
  対応するなら `xlrd<2.0` か LibreOffice 変換が要るので、1件のために依存を増やさない判断。
- 同一データセットに **CSVとXLSXの両方**がぶら下がっていることがある（須賀川市）。
  どちらも読んで `split_by_pref` の重複排除に任せている。

### XLSXで拾えるようになった主なもの

| 自治体 | データセット | 行数 | 原データの座標 |
|---|---|---:|---|
| 東京消防庁 | 消火栓及び防火水槽等（消火栓） | 133,956 | あり |
| 東京消防庁 | 消火栓及び防火水槽等（防火水槽等） | 44,305 | あり |
| 福岡市 | 消防水利施設一覧 | 19,769 | なし（住所のみ） |
| 豊田市 | 消火栓一覧 | 8,674 | なし（住所のみ） |
| 宇治市 | 消防水利 | 3,731 | なし（住所のみ） |
| 近江八幡市 | 消防水利施設一覧 | 2,414 | なし（住所が市までしか無い） |
| 豊田市 | 防火水槽一覧 | 1,975 | なし（住所のみ） |
| 紀美野町 | 消防水利施設一覧 | 604 | あり |
| 川南町 | 消防水利施設一覧 | 378 | あり |
| 久山町 | 消火栓 | 211 | あり |

### 名称・住所まわりで一緒に直したこと

- **「設置場所」を施設名にしない**。豊田市の `設置場所` は「歩道」「車道」で、そのまま名称にすると
  地図に「歩道」が8,674個並ぶ。名称の決め方を
  「施設名の列 → 種別の列（`KIND_EXACT`）→ 設置場所 → 種別の既定名」の順に変更した。
- **ジオコーディングに投げる住所を県・市区町村まで補う**（`geocode_query`）。
  福岡市の消防水利は所在地が「西区姪の浜4丁目0004番地1号」で県も市も入っておらず、
  そのまま国土地理院APIに投げると新潟市西区などに当たって**別の県に打ってしまう**。
  既存の `facilities_geocache.json`（住所そのままがキー）を無駄にしないため、
  キャッシュに当たらないときだけ補って問い合わせる。

## 7. 住所しかないデータの座標補完

原データに座標が無い行は国土地理院 AddressSearch (`https://msearch.gsi.go.jp/address-search/AddressSearch`) で補完する。
既存の `crawler.geocode.Geocoder` をそのまま使う（1req/s・市区町村より粗い結果は棄却するロジック込み）。
キャッシュだけ `data/facilities_geocache.json` に分けてコミットし、月次実行で再問い合わせしないようにしている。

- 補完したレコードには **`g: 1`** を立てる。アプリ側で「おおよその位置」として区別できる（SPEC 11章のリスク対策）。
- 亀山市のように**所在地が町丁目までしか無い**自治体では、同一町丁目の消火栓が全部同じ座標に積み上がる。
  そのため `split_by_pref` で (県, 種別, 緯度, 経度, 名称) が同じレコードを1件にまとめている。
- 1回の実行で新規に問い合わせる住所は既定 **2000件**まで（`--geocode-limit` / workflow の入力で変更可）。
  キャッシュ済みの住所は上限を消費しないので、**月次実行を重ねるほど補完率が上がる**設計。
- 今回は3回に分けて計 **8,210件** の住所をキャッシュに貯め、座標なし15,809件のうち15,587件（98.6%）を補完できた。
  キャッシュを含む状態なら1回の実行は約10分で終わる。初回だけ既定2000件では足りないので
  `--geocode-limit` を大きくするか、月次実行を数回まわして収束させる。

## 8. 運用

- `.github/workflows/facilities.yml` … 毎月1日 JST 04:00（`0 19 1 * *`）＋ `workflow_dispatch`。
  差分があれば `livecam-jp-bot` でコミットし、`publish.yml` を明示起動する（`shelters.yml` と同じ手順）。
  `data/facilities/`・`data/facilities_state.json`・`data/facilities_geocache.json` をコミット対象にしている。
- `publish.yml` の trigger paths に `data/facilities/**` を追加済み。
- `site/build.py` が `tools.facilities.sync_site()` を呼んで `data/facilities/` → `site/v1/facilities/` にコピーする。
  **publish環境には requests が無い**ので `tools/facilities.py` のトップレベルは標準ライブラリのみ、
  `requests`/`truststore`/`crawler.geocode` はすべて遅延importにしてある。
- 台帳の公開は全ユーザーに再取得を発生させるため、月1回にまとめている（CLAUDE.md「定期実行の知見」）。

## 9. 今後の拡張手順

1. **ポータルを増やす** … `PORTALS` に `{"key","name","base","delay","default_pref"/"org_name_is_jis"}` を1行足すだけ。
   追加前に必ず `curl https://<host>/robots.txt` で `/api/` が許可されているか確認する。拒否なら `BLOCKED_PORTALS` へ。
2. **キーワードを増やす** … `SEARCH_QUERIES` に足し、対応する種別を `KIND_TITLE_KEYWORDS` に足す。
   タイトル判定を変えたら `tools/tests/test_facilities.py::test_classify_*` にケースを足すこと。
3. ~~**XLSX に対応する**~~ … **2026-08-31 完了**（6.5章）。148,871件 → 339,670件。
4. **dataeye系5ポータル（埼玉・岡山・広島・島根・宮城）のライセンスを確認する** … 2.4章。
   未カバー県を5つ埋められるが、パッケージにライセンス欄が無いので現状は採用できない。
   **いま一番費用対効果が高いのはここ**。
5. **座標補完を進める** … 今回 30,470件が上限で見送りになっている（福岡市・豊田市・宇治市の住所のみデータ）。
   月次実行を数回まわすか `--geocode-limit` を上げれば自動的に増える（キャッシュ済みは上限を消費しない）。
6. **HTMLリソースに対応する** … 清瀬市・日野市・阿武町はリソース形式が HTML（自治体サイトの表ページ）。個別パーサが要るので優先度は低い。
7. **旧形式XLS（厚木市1件）** … `xlrd<2.0` か LibreOffice 変換が要る。1件のために依存は増やさない判断。
8. **札幌市・横浜市ほか16ポータル** … robots.txt の許諾を得られたら `BLOCKED_PORTALS` から `PORTALS` へ移すだけ（2.2章）。
9. **カバーされていない県** … 下表参照。BODIKに参加していない自治体は独自ポータル or PDF/HTML公開が多い。
   都道府県単位で「消防水利施設一覧 オープンデータ」を当たり、CKANがあればポータルを足すのが最短
   （ただし2.2章のとおり素のCKANはほぼ robots.txt で `/api/` が禁止されている）。

## 10. 実行結果（2026-08-31 第2回 / XLSX対応・山口県追加）

実行: `python -m tools.facilities --geocode-limit 6000`（version `2026-08-31T10:41:21Z`）

### before / after

| | 初回（CSVのみ・2ポータル） | 今回（XLSX対応・3ポータル） | 差 |
|---|---:|---:|---:|
| 採用データセット | 90 | 108 | +18 |
| 除外データセット | 17 | 17 | +0 |
| 配信レコード | 148,871 | 339,670 | +190,799 |
| カバー都道府県 | 22 / 47 | 24 / 47 | +2 |
| カバー自治体 | 78 | 91 | +13 |
| 種別 消防水利 | 147,885 | 338,656 | +190,771 |
| 種別 給水拠点 | 877 | 905 | +28 |
| 種別 備蓄倉庫 | 109 | 109 | +0 |
| 座標補完済み(`g:1`) | 15,587 | 26,312 | +10,725 |

座標補完: 対象 57,005件 → 補完 26,312 / 失敗 223 / 今回の上限(6,000件)で見送り 30,470。座標が得られず配信対象外 30,693件。

### 県別カバー状況

| JIS | 都道府県 | 件数 | 自治体数 | water | stock | fire_water | 初回比 |
|---|---|---:|---:|---:|---:|---:|---:|
| 01 | 北海道 | — | — | | | | |
| 02 | 青森県 | — | — | | | | |
| 03 | 岩手県 | — | — | | | | |
| 04 | 宮城県 | — | — | | | | |
| 05 | 秋田県 | 845 | 1 | 0 | 0 | 845 | +0 |
| 06 | 山形県 | — | — | | | | |
| 07 | 福島県 | 1,697 | 1 | 0 | 0 | 1,697 | +0 |
| 08 | 茨城県 | — | — | | | | |
| 09 | 栃木県 | — | — | | | | |
| 10 | 群馬県 | 5,415 | 1 | 0 | 0 | 5,415 | +0 |
| 11 | 埼玉県 | — | — | | | | |
| 12 | 千葉県 | 7,900 | 1 | 28 | 9 | 7,863 | +27 |
| 13 | 東京都 | 185,071 | 15 | 222 | 29 | 184,820 | +178,242 |
| 14 | 神奈川県 | 187 | 1 | 0 | 0 | 187 | +0 |
| 15 | 新潟県 | — | — | | | | |
| 16 | 富山県 | — | — | | | | |
| 17 | 石川県 | — | — | | | | |
| 18 | 福井県 | — | — | | | | |
| 19 | 山梨県 | — | — | | | | |
| 20 | 長野県 | 116 | 1 | 0 | 0 | 116 | +0 |
| 21 | 岐阜県 | — | — | | | | |
| 22 | 静岡県 | 2,507 | 1 | 0 | 0 | 2,507 | +0 |
| 23 | 愛知県 | 6,681 | 2 | 655 | 0 | 6,026 | +4,188 |
| 24 | 三重県 | 8,496 | 4 | 0 | 0 | 8,496 | +0 |
| 25 | 滋賀県 | 1,217 | 3 | 0 | 71 | 1,146 | +0 |
| 26 | 京都府 | 8,228 | 2 | 0 | 0 | 8,228 | +19 |
| 27 | 大阪府 | 19,594 | 14 | 0 | 0 | 19,594 | +0 |
| 28 | 兵庫県 | 10,890 | 2 | 0 | 0 | 10,890 | +0 |
| 29 | 奈良県 | 3,826 | 2 | 0 | 0 | 3,826 | +0 |
| 30 | 和歌山県 | 6,315 | 3 | 0 | 0 | 6,315 | +603 |
| 31 | 鳥取県 | — | — | | | | |
| 32 | 島根県 | — | — | | | | |
| 33 | 岡山県 | — | — | | | | |
| 34 | 広島県 | — | — | | | | |
| 35 | 山口県 | 6,981 | 4 | 0 | 0 | 6,981 | **新規** |
| 36 | 徳島県 | — | — | | | | |
| 37 | 香川県 | — | — | | | | |
| 38 | 愛媛県 | — | — | | | | |
| 39 | 高知県 | — | — | | | | |
| 40 | 福岡県 | 32,975 | 4 | 0 | 0 | 32,975 | +211 |
| 41 | 佐賀県 | 1,704 | 3 | 0 | 0 | 1,704 | +0 |
| 42 | 長崎県 | 7,911 | 5 | 0 | 0 | 7,911 | +0 |
| 43 | 熊本県 | 6,322 | 11 | 0 | 0 | 6,322 | +72 |
| 44 | 大分県 | — | — | | | | |
| 45 | 宮崎県 | 378 | 1 | 0 | 0 | 378 | **新規** |
| 46 | 鹿児島県 | 8,975 | 7 | 0 | 0 | 8,975 | +78 |
| 47 | 沖縄県 | 5,439 | 4 | 0 | 0 | 5,439 | +0 |

未カバー(23県): 北海道、青森県、岩手県、宮城県、山形県、茨城県、栃木県、埼玉県、新潟県、富山県、石川県、福井県、山梨県、岐阜県、鳥取県、島根県、岡山県、広島県、徳島県、香川県、愛媛県、高知県、大分県

### ライセンス内訳（採用分すべて再配布可）

| license_id | 判定ラベル | 件数 |
|---|---|---:|
| `cc-by-40-intl` | CC BY | 79 |
| `CC-BY-4.0` | CC BY | 19 |
| `cc-by` | CC BY | 8 |
| `cc-by-21-jp` | CC BY | 1 |
| `PDL1.0` | PDL 1.0 | 1 |

### 除外したデータセット

| ポータル | 自治体 | データセット | ライセンス | 除外理由 |
|---|---|---|---|---|
| bodik | 三田市 | 三田市避難所防災倉庫資機材一覧表 | CC BY | 有効な行が0件（XLSXにヘッダ行を持つ表が無い） |
| bodik | 厚木市 | 消防水利施設一覧 | CC BY | 有効な行が0件（旧形式のXLS（openpyxl非対応）） |
| bodik | 浦安市 | 浦安市防災倉庫設置場所一覧（オープンデータ） | CC BY | 表形式リソースなし（PDF） |
| tokyo | 日野市 | 消防水利施設一覧 | CC BY | 表形式リソースなし（HTML） |
| tokyo | 清瀬市 | 消防水利施設一覧 | CC BY | 表形式リソースなし（HTML） |
| tokyo | 清瀬市 | 応急給水拠点一覧 | CC BY | 表形式リソースなし（HTML） |
| yamaguchi | 上関町 | 【上関町】消防水利施設一覧 | notspecified License not specified | 再配布可と判定できないライセンス |
| yamaguchi | 光市 | 【光市】消防水利施設一覧 | CC BY | 表形式リソースなし（?） |
| yamaguchi | 周南市 | 【周南市】消防水利施設一覧 | notspecified License not specified | 再配布可と判定できないライセンス |
| yamaguchi | 周防大島町 | 消防水利施設一覧 | 不明 | ライセンス未記載 |
| yamaguchi | 和木町 | 【和木町】消防水利施設一覧 | 不明 | ライセンス未記載 |
| yamaguchi | 宇部市 | 【宇部市】消防水利施設一覧 | 不明 | ライセンス未記載 |
| yamaguchi | 岩国市 | 【岩国市】消防水利施設一覧 | 不明 | ライセンス未記載 |
| yamaguchi | 平生町 | 【平生町】消防水利施設一覧 | CC BY | 表形式リソースなし（?） |
| yamaguchi | 長門市 | 【長門市】消防水利施設一覧 | CC BY | 表形式リソースなし（?） |
| yamaguchi | 防府市 | 【防府市】消防水利施設一覧 | CC BY | 表形式リソースなし（?） |
| yamaguchi | 阿武町 | 【阿武町】消火栓一図 | CC BY | 表形式リソースなし（HTML） |

ライセンス起因の不採用は **6件**（山口県の `notspecified` / ライセンス未記載）。残りは形式・内容によるもの。

### 採用した自治体（91）

上天草市, 下松市, 下関市, 世田谷区, 中央区, 中野区, 丹波市, 久山町, 久留米市, 亀山市, 五島市, 前橋市, 北九州市, 南さつま市, 南九州市, 南阿蘇村, 南風原町, 名古屋市, 和歌山市, 和水町, 品川区, 四日市市, 垂水市, 堺市, 墨田区, 多久市, 多良木町, 大宜味村, 大東市, 大津市, 天草市, 奄美市, 宇治市, 宮崎市, 寝屋川市, 対馬市, 小城市, 小金井市, 山都町, 島原市, 川南町, 彦根市, 志布志市, 時津町, 杉並区, 東京消防庁, 東京都水道局, 東員町, 東大阪市, 東村山市, 柳井市, 橿原市, 江東区, 沖縄市, 津奈木町, 熊野市, 玄海町, 生駒市, 益城町, 目黒区, 真鶴町, 福岡市, 福知山市, 箕面市, 紀の川市, 紀美野町, 練馬区, 美祢市, 肝付町, 舞鶴市, 船橋市, 芦北町, 茅野市, 荒尾市, 豊中市, 豊岡市, 豊島区, 豊田市, 貝塚市, 近江八幡市, 那覇市, 長崎市, 阪南市, 霧島市, 青梅市, 静岡市, 須賀川市, 飯塚市, 高島市, 高森町, 鹿角市

うち今回の追加（13）: 下松市, 下関市, 久山町, 宇治市, 宮崎市, 川南町, 東京消防庁, 柳井市, 福岡市, 紀美野町, 美祢市, 近江八幡市, 青梅市

